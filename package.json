{
  "name": "calculatupizza",
  "version": "1.0.0",
  "private": true,
  "description": "Calculadora de masa de pizza napolitana (PWA estática, sin build).",
  "scripts": {
    "test": "node --test \"tests/**/*.test.js\""
  }
}
